---
title: "Set up agent support"
description: "Install the skill, slash commands, and the MCP server for Claude Code, Cursor, Copilot, Codex CLI, Antigravity, and OpenCode."
icon: "download"
---

Agent support turns a coding agent into an API-capable collaborator that can scan routes, enrich specs, build flows, and debug failures — all backed by reviewable markdown files in your repo.

<CardGroup cols={2}>
 <Card title="Skills" icon="book-open">
 A single markdown playbook that teaches agents the Trellis spec format, file layout, and how to choose the right approach for each task. All agents get the skill.
 </Card>
 <Card title="Slash commands" icon="terminal">
 Two `/trellis` commands for Claude Code and Codex CLI: `/trellis` for creating, enriching, and building flows; `/trellis-debug` for diagnosing failures.
 </Card>
 <Card title="MCP server" icon="plug">
 Structured tools for Claude Code: exec, flow, author, and more. Agents get typed results without parsing shell output.
 </Card>
 <Card title="Shared markdown source" icon="code-pull-request">
 Every agent change is a markdown file in `api-docs/`. Reviewable, diffable, committable. No hidden state.
 </Card>
</CardGroup>

---

## Install in one command

<Steps>
 <Step title="Install skill and slash commands">
 Run the installer from the root of your project. It auto-detects which agents are present and writes the correct files for each.

 ```bash
 trellis skills install
 ```

 The command writes the skill and (for Claude Code / Codex CLI) slash commands to the directories each agent reads.
 </Step>

 <Step title="Verify">
 ```bash
 trellis skills list
 ```

 Example output:

 ```
 claude-code detected .claude/skills/trellis/SKILL.md
                       .claude/commands/trellis.md
                       .claude/commands/trellis-debug.md
 cursor     detected   .cursor/rules/trellis.mdc
 copilot    detected   .github/instructions/trellis.instructions.md
 ```
 </Step>

 <Step title="Register the MCP server (Claude Code only)">
 The MCP server gives Claude Code structured access to spec execution, pipelines, and authoring — no terminal output parsing.

 ```bash
 claude mcp add trellis -- trellis mcp
 ```

 Verify:

 ```bash
 claude mcp list
 ```
 </Step>
</Steps>

---

## Per-agent install

<CodeGroup>

```bash Claude Code
trellis skills install --agent=claude-code
# Installs: 1 skill + 2 slash commands
# Locations: .claude/skills/trellis/, .claude/commands/
```

```bash Codex CLI
trellis skills install --agent=codex-cli
# Installs: 1 skill + 2 slash commands
# Locations: ~/.codex/skills/trellis/, ~/.codex/commands/
```

```bash Cursor
trellis skills install --agent=cursor
# Installs: 1 skill as .mdc rule
# Location: .cursor/rules/
```

```bash GitHub Copilot
trellis skills install --agent=copilot
# Installs: 1 skill as .instructions.md
# Location: .github/instructions/
```

```bash Antigravity
trellis skills install --agent=antigravity
# Installs: 1 skill
# Location: .agents/skills/
```

```bash OpenCode
trellis skills install --agent=opencode
# Installs: 1 skill
# Location: .opencode/skills/
```

</CodeGroup>

### What each agent gets

| Agent | Skills | Slash commands | MCP |
|---|---|---|---|
| Claude Code | 1 skill | 2 commands | ✓ |
| Codex CLI | 1 skill | 2 commands | |
| Cursor | 1 skill | | |
| GitHub Copilot | 1 skill | | |
| Antigravity | 1 skill | | |
| OpenCode | 1 skill | | |

---

## Keep skills current

After upgrading Trellis, check whether installed skills match the current binary:

```bash
trellis doctor
```

To update stale skills automatically:

```bash
trellis doctor --fix
```

<Warning>
 Stale skills are the most common cause of agents missing new commands or writing files in unexpected ways. Run `trellis doctor --fix` after every Trellis upgrade.
</Warning>
