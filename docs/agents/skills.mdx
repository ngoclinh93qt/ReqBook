---
title: "Skills reference"
description: "A single SKILL.md playbook installed for every supported agent — teaches the Trellis spec format, file layout, MCP tools, and when to use each command."
icon: "book-open"
---

Skills are markdown playbooks embedded in the Trellis binary. When installed, agents read them from their config directories and apply them automatically when a user prompt matches a known intent — you do not need to mention Trellis explicitly.

<Card title="trellis" icon="book-open">
Covers the full Trellis workflow: project layout, endpoint spec format, Assertions operators, pipeline capture patterns, MCP tool reference, and routing rules for when to use each slash command.
</Card>

---

## Install

```bash
trellis skills install
```

This writes the skill to every supported agent's config directory. To target one agent:

```bash
trellis skills install --agent=cursor
trellis skills install --agent=copilot
trellis skills install --agent=claude-code
```

---

## What the skill teaches

### Project layout

```
api-docs/
├── trellis.md              # project config (name, default-env, timeouts)
├── _shared/env.md          # base URLs and variables per environment
├── apis/<resource>/<method>-<slug>.md   # one file per endpoint
└── flows/<name>.md         # multi-step pipelines
```

### Endpoint format

Required frontmatter: `resource`, `protocol: http`, `method`, `path` (`:param` for path params), `version: 1`.

Sections in order: `## Request` → `## Expected response` → `## Error responses` (optional) → `## Assertions` (optional) → `## Tests` (optional) → `## Notes` (optional).

### Assertions

Structured rules that run after each execution:

| Operator | Example |
|---|---|
| _(equals)_ | `status: 200` · `body.name: Ada Lovelace` |
| `exists` | `body.id: exists` |
| `in [...]` | `body.role: in [admin, user]` |
| `contains` | `headers.content-type: contains json` |
| `matches` | `body.slug: matches ^[a-z]+$` |

### Pipeline capture patterns

| Pattern | When to use |
|---|---|
| `response.body.<field>` | Top-level JSON field |
| `response.body.<a>.<b>` | Nested JSON field |
| `response.body[<n>].<field>` | Field from nth array item |
| `response.headers.<name>` | Response header value |

### Routing table

The skill routes the agent to the right tool or command based on the situation:

| Situation | Action |
|---|---|
| Create or update specs | `/trellis` |
| Debug a failing endpoint or pipeline | `/trellis-debug` |
| Execute a spec | `trellis_exec` MCP tool |
| Run a pipeline | `trellis_flow` MCP tool |

---

## Supported agents

| Agent | Skill format | Path |
|---|---|---|
| Claude Code | `SKILL.md` | `.claude/skills/trellis/SKILL.md` |
| Cursor | `.mdc` | `.cursor/rules/trellis.mdc` |
| GitHub Copilot | `.instructions.md` | `.github/instructions/trellis.instructions.md` |
| Codex CLI | `SKILL.md` | `.codex/skills/trellis/SKILL.md` |
| Antigravity | `SKILL.md` | `.antigravity/skills/trellis/SKILL.md` |
| OpenCode | `SKILL.md` | `.opencode/skills/trellis/SKILL.md` |

All agents receive the same skill content — only the file format differs.
