# mark-api-down-md

Thin npm wrapper for the MarkApiDown Rust binary.

```bash
npx mark-api-down-md@latest version
```

The wrapper downloads the matching binary from GitHub Releases on first run and caches it in `~/.cache/mark-api-down-md/`.
