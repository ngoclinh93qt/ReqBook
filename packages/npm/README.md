# trellis-md

Thin npm wrapper for the Trellis Rust binary.

```bash
npx trellis-md@latest version
```

The wrapper downloads the matching binary from GitHub Releases on first run and caches it in `~/.cache/trellis-md/`.
